<h1> Book Index </h1>

 <% @books.each do |book| %>
 <%= link_to(book.title, book_ratings_path(book.id)) %>
 
<br>

<% end %> 
<br>

<%= link_to("Create New Book", new_book_path) %>
<br>
<%= link_to '<button type="button">Sort Alphabetically</button>'.html_safe, sort_path %>

